const appName = () => {
    console.log("The browser app name is: ", navigator.appCodeName);
}

appName();


const appVersion = () => {
    console.log("The browser app version is:",navigator.appVersion);
}

appVersion();